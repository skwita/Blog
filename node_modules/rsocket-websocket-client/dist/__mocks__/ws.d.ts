/// <reference types="jest" />
import { CloseEvent, ErrorEvent, MessageEvent } from "ws";
declare const EventEmitter: any;
export declare class MockSocket extends EventEmitter {
    send: jest.Mock<any, any>;
    close: jest.Mock<any, any>;
    addEventListener: (name: any, handler: any) => void;
    removeEventListener: (name: any, handler: any) => void;
    mock: {
        close: (event: Partial<CloseEvent>) => void;
        open: () => void;
        message: (message: Partial<MessageEvent>) => void;
        error: (event: Partial<ErrorEvent>) => void;
    };
}
export {};
